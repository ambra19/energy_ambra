name: energy
author: ambra
source: https://github.com/ambra19/energy_ambra
version: 0.0.1
description: A service that publishes energy data of the Rover

commands:
  build: make build
  run: ./bin/energy

inputs:

outputs:
  - energy

configuration:
  - name: updates-per-second
    type: number
    value: 1000
    tunable: true
